# ============================================
# Frontend Environment Variables Example
# ============================================
# Copy this file to .env and fill in your actual values
# Vite only exposes variables prefixed with VITE_ to the client
# Access them in code using: import.meta.env.VITE_*
# ============================================

# ========== API Configuration ==========
# Backend API base URL (used in vite.config.ts proxy)
# For development, this is used by Vite proxy
VITE_API_BASE_URL=http://localhost:8084

# ========== OAuth2/Keycloak Configuration ==========
# Keycloak Server Configuration
VITE_KEYCLOAK_AUTHORITY=http://localhost:8081/realms/event-tickets-platform
VITE_KEYCLOAK_CLIENT_ID=event-tickets-platform-app

# Frontend URL for OAuth redirect
# In development, this is typically http://localhost:5173
# In production, this should be your deployed frontend URL
VITE_REDIRECT_URI=http://localhost:5173/callback

# ========== Application Configuration ==========
# Application name
VITE_APP_NAME=Event Tickets Platform

# ========== Development Configuration ==========
# Vite dev server port (default: 5173)
VITE_PORT=5173

# ========== Production Configuration ==========
# Base path for production builds (usually "/" when served from Spring Boot)
VITE_BASE_PATH=/

# ========== Feature Flags (Optional) ==========
# Enable/disable features
# VITE_ENABLE_DEBUG_LOGS=true
# VITE_ENABLE_ANALYTICS=false
